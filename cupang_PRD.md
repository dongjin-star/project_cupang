# PRD — 쿠팡 상품등록 어시스턴트

> 도매 사이트 상품 정보를 넣으면, 쿠팡 WING 상품등록 폼의 모든 입력값을 AI가 추천해주는 웹앱

---

## 1. 배경

쿠팡 로켓그로스 상품등록 폼은 입력 항목이 60개 이상이다.
대부분은 상품마다 판단이 반복되고, 실수 하나가 **판매중지·검수 반려·마진 적자**로 이어진다.

실제로 겪은 실수 사례:

| 실수 | 결과 |
|---|---|
| 옵션 사이즈를 "소형"으로 입력 | 같은 가격이 비싸 보임 → 전환율 하락 |
| 소재를 "아크릴"로 표기 (실제 PET) | 표시 오류 |
| "식기세척기 사용 가능" 표기 | 변형 반품이 판매자 귀책 |
| 마진 계산에서 부가세 10% 누락 | 마진 3,757원 → 실제 2,584원 |
| 기둥 개수 오표기 | 물류센터 검수 반려 |

이 판단들은 **규칙화가 가능하다.** 그걸 자동화한다.

---

## 2. 목표

**한 문장:** 도매 상품 정보를 넣으면, 쿠팡 등록 폼에 그대로 복사할 수 있는 값 세트를 받는다.

### 성공 기준

- 등록 소요 시간 60분 → **15분 이내**
- 정책 위반 문구가 등록 전에 **100% 걸러짐**
- 마진이 음수인 가격을 등록하는 일이 **0건**

---

## 3. 사용자

1인 이커머스 셀러. 도매꾹·오너클랜 등에서 사입해 쿠팡에 등록한다.
디자인·개발 지식은 없고, 쿠팡 정책은 부분적으로만 안다.

---

## 4. 범위

### 포함 (V1)

| 영역 | 산출물 |
|---|---|
| 판매방식 | 로켓그로스 / 판매자배송 추천 + 근거 |
| 노출상품명 | 100자 이내, 3개 안 제시 |
| 등록상품명 | 판매자 관리용 코드 |
| 카테고리 | 쿠팡 카테고리 경로 + 수수료율 |
| 옵션 | 사이즈·색상·수량 값 + 라벨링 원칙 적용 |
| 가격 | 정상가·판매가 + **마진 계산기** |
| 상품 주요 정보 | 제조사, 인증, 병행수입, 연령, 부가세 등 8개 항목 |
| 검색어 | 태그 20개 (구조화된 조합) |
| 검색필터 | 항목별 추천값 |
| 상품정보제공고시 | 카테고리별 항목 자동 매핑 |
| 정책 검사 | 금지어·위험 표현 탐지 |

### 제외 (V1)

- 대표이미지 / 추가이미지 생성
- 상세페이지 제작
- WING 자동 입력 (브라우저 확장)
- 쿠팡 경쟁사 크롤링 — **봇 차단으로 불가.** 사용자가 캡쳐로 올리는 방식만 지원

---

## 5. 핵심 플로우

```
[1] 입력
     도매 URL 붙여넣기  또는  캡쳐 이미지 업로드  또는  수기 입력
                    ↓
[2] 상품 정보 추출
     품명 / 도매가 / 소재 / 크기 / 구성품 / 원산지 / 배송비
     → 사용자가 확인·수정 (이 단계에서 오류를 잡는 게 핵심)
                    ↓
[3] 원가 입력
     도매가 · 입고 배송비 · 예상 입고 수량 · 로켓그로스 물류비
                    ↓
[4] AI 생성
     전 항목 추천값 + 근거 + 대안
                    ↓
[5] 검수
     정책 위반 경고 · 마진 경고 · 미입력 항목
                    ↓
[6] 출력
     항목별 복사 버튼 / 전체 JSON / 체크리스트 인쇄
```

---

## 6. 화면

### 6.1 입력 화면

```
┌──────────────────────────────────────────┐
│  상품 정보 입력                            │
│                                          │
│  ○ 도매 사이트 URL                        │
│    [ https://domeggook.com/...        ]  │
│                                          │
│  ○ 이미지 업로드 (상품 페이지 캡쳐)         │
│    [ 파일 선택 · 드래그 ]                  │
│                                          │
│  ○ 직접 입력                              │
│                                          │
│              [ 분석 시작 ]                │
└──────────────────────────────────────────┘
```

### 6.2 확인·보정 화면

추출된 정보를 표로 보여주고 인라인 수정. **여기가 가장 중요한 화면이다.**
AI가 소재를 "아크릴"로 잘못 읽었을 때 사용자가 "PET"으로 고칠 수 있어야 한다.

각 필드에 신뢰도 표시:

| 필드 | 값 | 신뢰도 |
|---|---|---|
| 품명 | 회전형 투명 2단 트레이 | 높음 |
| 소재 | PET, PVC, 스테인리스 | **낮음 — 확인 필요** |
| 크기 | 지름 23cm × 높이 22cm | 높음 |

### 6.3 결과 화면

WING 폼과 **같은 순서**로 14개 섹션을 배열한다. 위에서 아래로 훑으며 복사만 하면 되도록.
섹션별 상세 출력값은 **7장 출력 명세** 참조.

```
① 카탈로그 매칭      ⑧ 상품 주요 정보
② 판매방식 선택      ⑨ 검색어
③ 노출상품명         ⑩ 검색필터
④ 카테고리           ⑪ 상품정보제공고시
⑤ 옵션               ⑫ 구비서류
⑥ 상품이미지 (범위 외) ⑬ 로켓그로스 물류 입고 정보
⑦ 상세설명 (범위 외)   ⑭ 입고 검수 기준
```

각 항목 카드 구성:
- 추천값 + 복사 버튼
- 근거 한 줄
- 대안 (있는 경우 2~3개)
- 경고 (있는 경우)

화면 하단에 **미완료 항목 목록**을 표시한다.

### 6.4 마진 계산기 (상시 노출)

가격을 바꾸면 즉시 재계산되는 사이드 패널.

---

## 7. 출력 명세 — WING 폼 순서

앱은 아래 **14개 섹션을 이 순서 그대로** 출력한다.
사용자가 WING 화면을 위에서 아래로 훑으며 복사만 하면 되도록 순서를 일치시킨다.

각 섹션 카드의 공통 구조:

```
┌─────────────────────────────────────────┐
│ ③ 노출상품명                    [ 복사 ] │
│                                         │
│ 회전형 투명 트레이 2단 원형 회전 수납함…  │
│                              62 / 100자 │
│                                         │
│ 근거: 용도 키워드 5개로 검색 진입점 확보  │
│ 대안: B안 · C안 보기 ▾                   │
│ ⚠ 없음                                  │
└─────────────────────────────────────────┘
```

---

### ① 카탈로그 매칭하기

| 출력 | 내용 |
|---|---|
| 값 | `건너뜀` |
| 근거 | 신규 상품은 매칭할 기존 카탈로그가 없음 |

기존 카탈로그와 매칭 가능한 상품인지 판단이 어려우므로, V1은 항상 건너뜀으로 안내한다.

---

### ② 판매방식 선택

| 출력 | 내용 |
|---|---|
| 값 | `로켓그로스` 또는 `판매자배송` |
| 근거 | 상품 부피·단가·회전율 기준 판단 |

**판단 규칙**

| 조건 | 추천 |
|---|---|
| 단가 10,000원 이상 + 부피 중간 이하 | 로켓그로스 |
| 단가 5,000원 미만 | 판매자배송 (물류비가 마진을 잠식) |
| 파손 위험 높음 | 판매자배송 |
| 사용자가 이미 선택함 | 그대로 유지 |

---

### ③ 노출상품명

| 필드 | 출력 |
|---|---|
| 브랜드 | `브랜드 없음(또는 자체제작)` 체크 여부 |
| 노출상품명 | **3개 안** (A·B·C), 각 100자 이내, 글자수 표시 |
| 등록상품명(판매자관리용) | `[사이트][상품번호]_[축약명]` |

**생성 규칙**

- 구조: `상품 유형 + 핵심 특징 + 용도 키워드 3~5개`
- 3개 안은 **키워드 조합을 달리** 한다 (같은 말 재배열 금지)
- 금지: 타사 브랜드명, 최상급 표현, 특수문자 남발

```
A. 회전형 투명 트레이 2단 원형 회전 수납함 냉장고 주방 화장대 약통 조미료 정리함
B. 2단 회전 트레이 투명 원형 조미료 정리대 냉장고 상부장 화장대 수납
C. 투명 회전 수납함 2단 360도 주방 조미료 화장품 약통 다용도 정리 트레이

등록상품명: 도매꾹63790470_회전트레이2단
```

---

### ④ 카테고리

| 필드 | 출력 |
|---|---|
| 카테고리 경로 | 전체 경로 (`>` 구분) |
| 판매수수료 | % + `VAT 별도, 정률` 표기 |
| 대안 | 후보 카테고리 1~2개 + 각 수수료율 |

```
주방용품 > 주방수납/정리 > 냉장고/냉동실정리용품 > 정리수납함/트레이
판매수수료 10.8% (VAT 별도, 정률)
```

수수료율은 **마진 계산에 그대로 연결**된다. 카테고리를 바꾸면 마진이 재계산되어야 한다.

---

### ⑤ 옵션

| 필드 | 출력 |
|---|---|
| 사이즈 | 옵션값 |
| 색상 | `투명(색상)` 형식 |
| 수량 | 숫자 + 단위 |
| 옵션명 결과 | 조합 결과 미리보기 |
| 정상가 | 원 |
| 판매가 | 원 |
| 재고수량 | 권장 초기 수량 |

**⭐ 사이즈 라벨링 원칙**

> 크기 라벨은 가치 인식에 직접 영향을 준다.
> **크기 형용사 대신 구조·수치 라벨을 쓴다.**

| 나쁨 | 좋음 | 이유 |
|---|---|---|
| 소형 | 2단 | 구성을 설명 |
| 대형 | 지름 27cm | 정보 제공 |
| Free | (사용 금지) | 정보량 0 |

`소형`·`미니`·`Free`가 생성되면 **경고를 띄우고 대안을 제시**한다.

```
사이즈  2단
색상    투명(색상)
수량    1개
→ 옵션명: 2단, 투명, 1개

정상가  16,900원
판매가  12,900원
재고    10~20개 (초기 권장)
```

---

### ⑥ 상품이미지

| 출력 | 내용 |
|---|---|
| 값 | `범위 외 — 별도 준비` |
| 안내 | 권장 1,000×1,000px / 최소 500px / 10MB 이하 / 대표 1장 + 추가 최대 9장 |

V1은 생성하지 않고 **규격만 안내**한다.
로켓그로스는 물류센터에서 실물과 이미지를 대조하므로, 실사 사용을 권장하는 문구를 함께 표시한다.

---

### ⑦ 상세설명

| 출력 | 내용 |
|---|---|
| 값 | `범위 외 — 별도 준비` |
| 안내 | 권장 780 × 5,000px / 10MB 이하 / JPG·PNG |

---

### ⑧ 상품 주요 정보

8개 항목을 표로 출력한다.

| 항목 | 값 | 판단 기준 |
|---|---|---|
| 제조사 | `수입산` 또는 공란 | **불명이면 회사명을 지어내지 않는다** |
| 상품 구성 | 동일한 상품으로 구성됨 | 번들이면 혼합 선택 + 번들 정책 경고 |
| 인증정보 | 인증·신고 대상 아님 | 카테고리·소재 기반 판단 |
| 병행수입 | 병행수입 아님 | 기본값 |
| 구매 연령 | 전체 연령 | 성인용품·주류만 예외 |
| 인당 최대구매수량 | 설정안함 | 기본값 |
| 판매기간 | 설정안함 | 기본값 |
| 부가세 | **과세** | 공산품은 과세. 면세는 농축수산물·도서 등 |

**부가세 항목에는 항상 안내 문구를 붙인다:**
> 판매가에 부가세 10%가 포함됩니다. 마진 계산에 반영되어 있습니다.

---

### ⑨ 검색어 (태그)

| 필드 | 출력 |
|---|---|
| 태그 | 20개, 쉼표 구분 문자열 (통째로 복사 가능) |
| 구성 분석 | 유형별 개수 표시 |

**구성 비율**

| 유형 | 개수 |
|---|---|
| 핵심 대표어 | 3 |
| 용도별 | 8 |
| 속성 | 3 |
| 롱테일 | 6 |

금지: 타사 브랜드명, 상품과 무관한 인기 키워드

```
회전트레이, 2단회전트레이, 회전선반, 회전수납, 조미료정리대, 조미료선반,
냉장고정리용품, 냉장고선반, 주방정리함, 주방수납, 화장품정리대, 화장대정리,
약통정리함, 욕실정리대, 투명수납함, 360도회전, 원형트레이, 상부장정리,
싱크대정리, 다용도트레이
```

---

### ⑩ 검색필터

카테고리마다 필터 항목이 다르다. **드롭다운형**과 **직접입력형**을 나눠 출력한다.

**드롭다운형** — 값 + 선택 근거

| 항목 | 값 |
|---|---|
| 수납정리공간 위치 | 냉장고 |
| 냉장고정리용품 종류 | 트레이 / 회전트레이 |
| 냉장고정리용품 용도 | 다용도 |
| 칸막이 여부 | 없음 |
| 손잡이 유무 | 없음 |
| 구성 | 1개 |
| 뚜껑포함여부 | 미포함 |
| 수납/정리용품 재질 | 플라스틱 (PET) |
| 스타일 | 모던 / 심플 |
| 품목 모양 | 원형 |
| 마감 유형 | 투명 |
| 방수 성능 | 방수 |
| 세척 용이성 | **손세척** |
| 수납가능여부 | 가능 |
| 미끄럼방지 가능여부 | 없음 |

**직접입력형** — 치수는 확정 정보에서 자동 계산

| 항목 | 값 |
|---|---|
| 세로길이 | 22 |
| 항목 길이가 더 긴 가장자리 | 23 |
| 항목 너비가 짧은 가장자리 | 23 |
| 아이템 높이 | 22 |

**공란 처리** — 이유를 함께 표시

| 항목 | 이유 |
|---|---|
| Manufacturer Part Number | 제조사 부품번호 없음 |
| Parent Manufacturer Part Number | 없음 |
| Global Trade Item Number | 바코드(GTIN/EAN) 없음 |

**⚠️ 소재 연동 경고**
소재가 PET·PVC·PP면 `세척 용이성`에 **식기세척기 관련 값을 선택하지 못하도록 차단**한다.

**⚠️ 실물 확인 필요 항목**
`미끄럼방지 가능여부`처럼 사진으로 판단이 어려운 항목은 `확인 필요` 배지를 붙인다.

---

### ⑪ 상품정보제공고시

카테고리별 항목 세트가 다르므로 **매핑 테이블**을 데이터로 보유한다.
(예: 주방용품은 10개 항목)

| 고시정보 명 | 값 |
|---|---|
| 품명 및 모델명 | 회전형 투명 2단 트레이 |
| 재질 | PET, PVC, 스테인리스 |
| 구성품 | 트레이 2개, 기둥 3개, 중앙 고정캡 1개 |
| 크기 | 지름 23cm × 높이 22cm |
| 출시년월 | 2026-08 |
| 제조자(수입자) | 수입산 / 판매자 |
| 제조국 | 중국 |
| 수입신고 문구 여부 | 해당없음 |
| 품질보증기준 | 제품 이상 시 공정거래위원회 고시 소비자분쟁해결기준에 의거 보상합니다. |
| A/S 책임자와 전화번호 | **사용자 입력 필요** |

**특별 규칙**

| 항목 | 규칙 |
|---|---|
| 제조자(수입자) | 불명이면 `수입산`. 회사명을 지어내지 않는다 |
| 수입신고 문구 | 식품 직접 접촉 기구가 아니면 `해당없음` |
| 품질보증기준 | 공정위 문구 고정 |
| **A/S 책임자** | **도매 공급사 번호 자동 차단.** 안심번호(050/0507) 안내 |

A/S 항목은 자동 생성하지 않고 **입력 요청 카드**로 띄운다.

> 도매 공급사 번호를 넣으면 고객이 직접 연락하게 되어 거래에 문제가 생깁니다.
> 개인번호 노출을 피하려면 안심번호(050/0507)를 권장합니다.

---

### ⑫ 구비서류

| 출력 | 내용 |
|---|---|
| 값 | `없음` 또는 필요 서류 목록 |
| 근거 | 인증정보 선택값과 연동 |

⑧에서 `인증·신고 대상 아님`을 선택했으면 자동으로 `없음`이 된다.

---

### ⑬ 로켓그로스 물류 입고 정보

| 출력 | 내용 |
|---|---|
| 값 | `나중에 입력` |
| 근거 | 입고 수량·날짜 확정 후 별도 프로세스에서 입력 |

판매가가 미확정이면 이 값을 고정한다.

---

### ⑭ 입고 검수 기준 체크리스트

체크박스로 출력한다. 상품 특성에 따라 항목이 달라진다.

| 확인 항목 | 상품 연동 조건 |
|---|---|
| 바코드 유무 — 없으면 직접 부착 | 항상 |
| 개별 포장 상태 | 항상 |
| 완충재 포함 여부 | 파손 위험 소재일 때 |
| 등록 이미지와 실물 일치 | 항상 |
| 구성품 수량 일치 (기둥 3개 등) | 구성품이 여러 개일 때 |

**구성품 수량은 확정 정보에서 끌어와 구체적으로 표시**한다.
"구성품 확인"이 아니라 "기둥 3개 확인"으로 출력해야 실수를 막는다.

---

### 결과 화면 하단 — 미완료 항목

출력이 끝나면 남은 작업을 모아 보여준다.

```
남은 작업 4개

1. 상품이미지 10장 준비
2. 상세설명 이미지 준비
3. A/S 연락처 확정 (안심번호 발급)
4. 로켓그로스 물류비 확인 → 판매가 확정
```

---

## 8. 마진 계산기

전 화면에 고정되는 사이드 패널. 판매가를 바꾸면 즉시 재계산된다.

### 계산식

```
순마진 = 판매가
        - (판매가 × 0.1)              ← 부가세 (과세 상품)
        - (판매가 × 카테고리 수수료율)   ← ④에서 연동
        - 도매 단가
        - (입고 배송비 ÷ 입고 수량)
        - 로켓그로스 물류비            ← 사용자 입력

환급 = (도매 단가 × 0.1 ÷ 1.1) + (쿠팡 수수료 × 0.1)   ← 일반과세자만
```

### 출력

| 항목 | 금액 |
|---|---|
| 판매가 | 12,900 |
| 부가세 | -1,173 |
| 쿠팡 수수료 (10.8%) | -1,393 |
| 도매 단가 | -7,600 |
| 입고 배송비 (÷20) | -150 |
| 로켓그로스 물류비 | -2,500 |
| **순마진** | **84원 (0.7%)** |

### 경고 조건

| 마진율 | 표시 |
|---|---|
| < 0% | 적자입니다 |
| < 10% | 광고 집행 시 적자로 전환됩니다 |
| < 15% | 얇습니다. 가격 재검토를 권장합니다 |

로켓그로스 물류비는 부피·무게로 결정되므로 **추정하지 않는다.**
미입력 시 마진을 `확정 불가`로 표시하고 WING 요금 계산기 링크를 안내한다.

---

## 9. 정책 검사기

생성된 모든 텍스트를 스캔한다.

| 패턴 | 등급 | 사유 |
|---|---|---|
| 최고, 1위, 최저가, 유일 | 차단 | 표시광고법 |
| 식기세척기 (PET/PVC/PP 상품) | 차단 | 변형 → 반품 귀책 |
| 열탕, 소독 가능 (플라스틱) | 차단 | 동일 |
| 식품용, 식품 직접 접촉 | 차단 | 인증 없이 표기 불가 |
| 카카오톡, 오픈채팅, 네이버, 외부 URL | 차단 | 외부 거래 유도 |
| 등록 브랜드명 목록 | 차단 | 상표 도용 |
| 소형, 미니 (옵션 라벨) | 경고 | 가치 인식 저하 |
| 만능, 완벽, 무조건 | 경고 | 과장 소지 |
| 도매 공급사 전화번호 | 차단 | A/S 책임 소재 문제 |

**소재 기반 동적 규칙:** 소재가 PET·PVC·PP면 내열 관련 표현을 전부 차단한다.

금지어 목록은 하드코딩하지 않고 **편집 가능한 JSON**으로 분리한다. 쿠팡 정책이 바뀌기 때문이다.

---

## 10. 데이터 모델

```ts
type Listing = {
  id: string
  source: { site: string; url?: string; capturedAt: string }

  // 1차 AI 추출 원본
  raw: {
    name: string
    wholesalePrice: number
    shippingFee: number
    material?: string
    size?: string
    components?: string
    origin?: string
    confidence: Record<string, 'high' | 'low'>
  }

  // 사용자 보정본 — 2차 생성의 입력
  confirmed: {
    name: string
    material: string
    size: { diameter?: number; width?: number; depth?: number; height: number }
    components: { label: string; count: number }[]
    origin: string
  }

  cost: {
    unitPrice: number
    inboundShipping: number
    inboundQty: number
    fulfillmentFee: number | null   // null이면 마진 확정 불가
    taxpayerType: 'general' | 'simplified'
  }

  // WING 폼 순서대로 — 출력 순서를 배열 인덱스로 보장
  output: {
    catalogMatch: { value: 'skip'; reason: string }

    salesMethod: { value: 'rocket_growth' | 'seller'; reason: string }

    displayName: {
      noBrand: boolean
      candidates: { label: 'A' | 'B' | 'C'; text: string; length: number }[]
      internalName: string
    }

    category: {
      path: string
      feeRate: number
      alternatives: { path: string; feeRate: number }[]
    }

    options: {
      size: string
      color: string
      qty: string
      combinedLabel: string
      listPrice: number
      salePrice: number
      stockHint: string
    }

    mainImage: { status: 'out_of_scope'; spec: string }
    detailPage: { status: 'out_of_scope'; spec: string }

    mainInfo: {
      manufacturer: string
      composition: 'same' | 'mixed'
      certification: string
      parallelImport: boolean
      ageLimit: string
      maxPerBuyer: string
      salePeriod: string
      vat: 'taxable' | 'exempt'
    }

    tags: {
      list: string[]              // 20개
      joined: string              // 복사용
      breakdown: { core: number; usage: number; attribute: number; longtail: number }
    }

    filters: {
      dropdowns: { key: string; value: string; needsVerify?: boolean }[]
      inputs: { key: string; value: string }[]
      blanks: { key: string; reason: string }[]
    }

    notice: {
      categoryKey: string         // 고시정보 카테고리
      fields: { key: string; value: string; requiresUserInput?: boolean }[]
    }

    documents: { value: 'none' | string[]; reason: string }

    fulfillmentInfo: { value: 'later'; reason: string }

    inboundChecklist: { item: string; checked: boolean }[]
  }

  margin: {
    breakdown: { label: string; amount: number }[]
    net: number | null
    rate: number | null
    level: 'ok' | 'thin' | 'risky' | 'loss' | 'unknown'
    refund: number
  }

  violations: {
    section: string               // output 내 경로
    text: string
    level: 'block' | 'warn'
    reason: string
    fix: string
  }[]

  todos: string[]                 // 미완료 항목
}
```

---
## 11. 기술 구성

| 영역 | 선택 |
|---|---|
| 프레임워크 | Next.js (App Router) |
| 언어 | TypeScript |
| 스타일 | Tailwind CSS |
| AI | Claude API (`claude-sonnet-4-6`) |
| 이미지 입력 | Claude vision (base64) |
| 저장 | V1은 localStorage · V2에서 DB |
| 배포 | Vercel |

### 도매 사이트 정보 추출

| 방법 | 비고 |
|---|---|
| URL 파싱 | 도매꾹은 fetch 가능. 사이트별 파서 필요 |
| **이미지 업로드** | **주력 방식.** 사이트 구조 변경에 영향받지 않음 |
| 수기 입력 | 최후 수단 |

> 크롤링은 사이트 정책과 robots.txt를 확인하고 사용한다.
> 이미지 업로드 방식이 안정적이고 법적 리스크도 낮으므로 이쪽을 기본으로 한다.

### AI 호출 설계

한 번에 전부 생성하지 않고 **2단계로 나눈다.**

```
1차: 이미지·URL → 상품 정보 구조화 (JSON)
     ↓ 사용자 확인
2차: 확정 정보 → 등록 항목 전체 생성 (JSON)
```

이유: 1차 추출이 틀리면 2차 결과가 전부 틀린다. 중간 확인이 필수다.

JSON 응답은 프롬프트에서 **마크다운 코드펜스 없이 순수 JSON만** 반환하도록 명시하고, 파싱 실패에 대비한 fallback을 둔다.

---

## 12. 개발 단계

### Phase 1 — 코어 (2주)

- 수기 입력 폼
- AI 생성: 상품명 / 옵션 / 검색어 / 고시정보
- 마진 계산기
- 정책 검사기
- 결과 화면 + 복사 버튼

> 이미지 추출 없이도 **가치가 나온다.** 여기서 먼저 검증한다.

### Phase 2 — 입력 자동화 (1주)

- 이미지 업로드 → Claude vision 추출
- 도매꾹 URL 파서
- 확인·보정 화면

### Phase 3 — 축적 (1주)

- 등록 이력 저장
- 카테고리별 수수료율 테이블
- 자주 쓰는 값 프리셋

### Phase 4 — 검토 대상

- 브라우저 확장으로 WING 자동 입력
- 경쟁사 캡쳐 분석 → 차별화 포인트 제안
- 상세페이지 HTML 생성 (별도 프로젝트와 연동)

---

## 13. 열린 질문

빌드 전에 정해야 할 것들.

1. **카테고리 수수료율 테이블을 어떻게 확보할 것인가**
   쿠팡 공개 자료가 제한적. 수동으로 쌓으면서 캐싱하는 방식이 현실적.

2. **로켓그로스 물류비를 추정할 수 있는가**
   부피·무게 구간표를 확보하면 자동 계산 가능. 없으면 계속 수동 입력.

3. **금지어 목록을 어떻게 유지할 것인가**
   쿠팡 정책이 바뀐다. 하드코딩 대신 편집 가능한 JSON으로 분리한다.

4. **다중 옵션 상품을 어떻게 다룰 것인가**
   소/대 2종처럼 옵션이 여러 개일 때 마진을 개별 계산해야 한다.

5. **검색필터 항목 세트를 어떻게 확보할 것인가**
   카테고리마다 항목과 선택지가 다르고, 드롭다운 값은 WING에서만 보인다.
   등록할 때마다 실제 화면의 항목·선택지를 앱에 저장해 카테고리별로 쌓는다.

6. **상품정보제공고시 카테고리 매핑을 어디까지 만들 것인가**
   고시정보 카테고리는 30종 이상이다. V1은 주방용품 등 실제 취급 카테고리
   3~5개만 만들고, 미지원 카테고리는 항목명을 사용자가 입력하도록 한다.

7. **Phase 1 검증을 무엇으로 판단할 것인가**
   같은 상품을 앱으로 등록했을 때와 수기로 등록했을 때의 소요 시간,
   그리고 정책 위반이 걸러진 건수를 기록해 비교한다.

---

## 14. 비범위 (명확히 하지 않는 것)

- 세무 판단 — 부가세 계산은 참고용이며, 사업자 유형별 정확한 처리는 세무 전문가 확인이 필요하다는 안내를 항상 표시한다
- 법률 자문 — 정책 검사기는 위험 신호를 알려줄 뿐, 최종 책임은 판매자에게 있다
- 쿠팡 계정 연동 — V1에서 다루지 않는다
